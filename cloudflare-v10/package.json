{
  "name": "genova-check",
  "private": true,
  "version": "1.0.0",
  "description": "Blexo Check",
  "scripts": {
    "deploy": "wrangler d1 migrations apply blexo-suite --remote && wrangler deploy",
    "migrate": "wrangler d1 migrations apply blexo-suite --remote",
    "typecheck": "wrangler types"
  },
  "devDependencies": {
    "wrangler": "^4.35.0"
  }
}
