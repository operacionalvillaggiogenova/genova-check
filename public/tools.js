const grid=document.getElementById('toolsGrid');const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));(async()=>{try{const {user}=await BlexoAuth.requireUser();const modules=(user.modules||[]).filter(m=>m.code!=='activities');grid.innerHTML=modules.length?modules.map(m=>`<a class="tool-card" href="${m.path}"><span class="tool-icon">${esc(m.icon||'•')}</span><span><strong>${esc(m.name)}</strong><small>${m.module_group==='ADMIN'?'Administração':m.module_group==='CORE'?'Operação':'Ferramenta de campo'}</small></span></a>`).join(''):'<p class="empty-state">Nenhuma ferramenta liberada.</p>'}catch(e){if(e.status!==401)grid.innerHTML=`<p class="empty-state">${esc(e.message||'Não foi possível carregar.')}</p>`}})();
